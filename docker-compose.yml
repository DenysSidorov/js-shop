version: '3'

services:
#  front:
#    build: ./front
#    command: npm run start
#    ports:
#      - "3002:3002"
   t-server:
     build: ./test-server
     command: npm run start
     ports:
       - "3000:3000"
