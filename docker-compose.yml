version: '3'

services:
#  front:
#    build: ./front
#    command: npm run start
#    ports:
#      - "3002:3002"
   t-server:
     build: ./test-server
     command: npm run start
     restart: unless-stopped
     ports:
       - "3000:3000"
     environment:
       - PORT=3000
       - HOST=http://my-prod.com/
