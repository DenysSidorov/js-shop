import React from "react";

class Card extends React.Component {
    componentDidMount(prevProps) {
        window.scrollTo(0, 0)
    }
    render() {
        return (
<div>CARD</div>
        )

    }
}

export default Card;